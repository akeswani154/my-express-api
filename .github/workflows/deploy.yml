name: Deploy my-express-api to AWS

on:
  push:
    branches:
      - develop

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ vars.EC2_HOST }}
          username: ubuntu
          key: ${{ vars.EC2_KEY }}
          script: |
            cd ~/MY-EXPRESS-API
            git pull origin develop
            npm install --production
            export MONGO_URI=${{ vars.MONGO_URI }}
            export PORT=${{ vars.PORT }}
            pm2 restart backend || pm2 start app.js --name backend
